export type TodoItemType = {
    id: number;
    userId: number;
    title: string;
    completed: boolean;
}

export type DropdownProps = {
    name: string;
    firstOption: string;
    secondOption: string;
}